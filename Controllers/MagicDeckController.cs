namespace MTGCardApi.Controllers;

public class MagicDeckController
{
    /*
     * 
     * TODO:
     * 
     * - Create endpoint
     *   - Creates a MagicDeck record
     *   - Create request object for creating a MagicDeck. Ensure Cards is nullable.
     * 
     * - Update deck Endpoint
     *   - Allow updates to name, description, format, and commander
     * 
     * - Delete deck endpoint
     * 
     * - Add Card(s) to deck
     * 
     * - Remove Card(s) from deck
     * 
     * */
}
